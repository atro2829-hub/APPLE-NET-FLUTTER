<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ─── Network Permissions ─── -->
    <!-- Required for Firebase connections, data processing, and all API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required to check internet connectivity before financial operations (purchases, deposits) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ─── Camera & Storage Permissions ─── -->
    <!-- Required for uploading deposit receipt images and network photos -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Required to read images from gallery for receipt/network photo upload -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 13+ granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ─── Optional Permissions ─── -->
    <!-- Haptic feedback for UI interactions -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Biometric authentication (fingerprint/face) for securing the app - optional -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- ─── Feature Declarations ─── -->
    <!-- Camera is not required - app works without it -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="false"
        android:supportsRtl="true"
        android:theme="@style/LaunchTheme">

        <!-- ─── Main Activity ─── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Flutter theme metadata -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Launch intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ─── Flutter Embedding Metadata ─── -->
        <!-- Used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ─── Firebase Messaging Default Notification Channel ─── -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="apple_net_notifications" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_name"
            android:value="Apple.NET Notifications" />

        <!-- ─── Firebase Default Notification Icon ─── -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <!-- ─── Firebase Default Notification Color ─── -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />

    </application>

    <!-- ─── Query Intent Filters ─── -->
    <!-- Required for text processing (Flutter ProcessTextPlugin) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Required for URL launcher (opening WhatsApp, phone dialer, etc.) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

</manifest>
